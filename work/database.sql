CREATE table monitoring_system(
  UID VARCHAR(10),
  PID INTEGER,
  PPID INTEGER,
  C INTEGER,
  SZ INTEGER,
  RSS INTEGER,
  PSR INTEGER,
  STIME VARCHAR(5),
  TTY VARCHAR(10),
  TIME VARCHAR(5),
  CMD VARCHAR(25),
  PRIMARY KEY (PID)
);